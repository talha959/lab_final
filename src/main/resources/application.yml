server:
  port: 9999

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/talha959/config-repo
          default-label: main
          search-paths: /
  security:
    user:
      name: talha
      password: muP2024410
